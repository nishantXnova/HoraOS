# Shared flags only. Machine-specific JDK pinning lives in ~/.gradle/gradle.properties
# (org.gradle.java.home) so CI + other machines use their own JDK 17.
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
